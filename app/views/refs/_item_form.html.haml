- if @item.errors.any?
  .alert.alert-danger
    %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} ×
    %strong= "errors: #{@item.errors.count}"
    %ul
      - @item.errors.full_messages.each do |x|
        %li= x
.row
  = form_for @item, :html => { :class => 'form-horizontal col-md-12' } do |f|
    = render partial: 'item_form_fields', :locals => { :form => f }
    .form-group
      .col-md-offset-2.col-md-8
        = f.submit 'apply', :class => 'btn btn-success'
      .col-md-2
        = link_to 'cancel', { :controller => @item.class.to_s.tableize, :action => 'index' }, :class => 'btn btn-default mx-btn-right'
